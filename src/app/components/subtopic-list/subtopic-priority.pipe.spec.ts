import { SubtopicPriorityPipe } from './subtopic-priority.pipe';

describe('SubtopicPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new SubtopicPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
